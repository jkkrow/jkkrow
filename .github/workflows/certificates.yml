name: Certificates

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-readme:
    name: Update README.md with certificates
    runs-on: ubuntu-latest
    steps:
      - uses: pemtajo/badge-readme@main
